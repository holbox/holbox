import CloudKit

class Shared {
//    private lazy var container = CKContainer(identifier: "iCloud.holbox").publicCloudDatabase
    
    func load(_ success: @escaping(Session.Global) -> Void, error: @escaping() -> Void) {
        
    }
}
