import CloudKit

class Shared {
//    private lazy var container = CKContainer(identifier: "iCloud.holbox").publicCloudDatabase
    
    func load(_ error: @escaping() -> Void, success: @escaping(Session.Global) -> Void) {
        
    }
}
